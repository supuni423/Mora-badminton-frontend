# umisf-client

## Environment variables

Create `.env` from the example and set your backend URL:

```bash
cp .env.example .env
```

- `REACT_APP_API_URL=http://localhost:3001/api` (local backend)
- `REACT_APP_API_URL=https://<your-vercel-app>.vercel.app/api` (production)
